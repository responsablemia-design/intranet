# ── Dominio ──────────────────────────────────────────────────────────────────
DOMAIN=iesfelixdeazara.com
ACME_EMAIL=admin@iesfelixdeazara.com

# ── Base de datos ─────────────────────────────────────────────────────────────
DB_ROOT_PASSWORD=cambia_esto_root
DB_NAME=intranet
DB_USER=intranet_user
DB_PASSWORD=cambia_esto_password

# ── Sesiones ──────────────────────────────────────────────────────────────────
# Genera uno seguro con: openssl rand -base64 48
SESSION_SECRET=cambia_esto_por_un_secreto_muy_largo_y_aleatorio

# ── Google OAuth2 ─────────────────────────────────────────────────────────────
# Obtén estas credenciales en: https://console.cloud.google.com
# Proyecto → APIs y servicios → Credenciales → Crear ID de cliente OAuth 2.0
GOOGLE_CLIENT_ID=tu_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=tu_client_secret
